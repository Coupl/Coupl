
import React from 'react';
import {
    Text,
} from 'react-native';

const MessagesScreen = ({ navigation }) => {
    return (
        <Text>Burası mesajlar ekranı</Text>
    );
};

export default MessagesScreen;
